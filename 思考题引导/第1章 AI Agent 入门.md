---
书名: 深入理解 AI Agent：设计原理与工程实践
章节: 1
章标题: AI Agent 入门
分册: 思考题引导
状态: 已录入（待自答）
阅读日期: 2026-09-08
tags:
  - AI-Agent
  - 精读笔记
---

# 第1章 AI Agent 入门（思考题引导）

> [!info] 使用说明
> 每题含：**题目原文**（按原书摘录，★=难度）、**提示思路**（只指方向、不剧透答案）、**我的回答**（留白区，供你先自答再对照）。参考答案位于原书 `reference-answers.md`「第一章 AI Agent 入门」节（文件在库外 `D:\ObsidianRepository\book\`，建议自答完成后才翻阅）。

---

### 思考题 1 ★★

> 如果你只能给一个 Agent 系统增加一项能力——更强的模型、更丰富的上下文还是更多的工具——你会选哪个？在什么条件下你的选择会改变？

**提示思路**：先判断当前系统的瓶颈缺口是哪类（知识/推理能力缺口？信息不可见？行动不可达？），再对照本书「能力杠杆」论述；别忘了成本、延迟与维护负担会随选择不同。

**我的回答**：

---

### 思考题 2 ★★★

> ReAct 循环中，累计缓存读取量随轮数近似二次方增长。如何降低这种增长？

**提示思路**：从「上下文 = 静态前缀 + 轨迹」出发拆解——哪些部分每次重复读取？哪些可以复用/增量/压缩？KV Cache 只缓存前缀，轨迹越长重复越多；想想第 2 章预告的压缩与隔离思路。

**我的回答**：

---

### 思考题 3 ★★

> “模型即 Agent” 范式意味着模型在工具调用决策上越来越自主。但本章论证了 Harness 工程的重要性反而在增加。这两个趋势如何共存？Agent 框架未来的核心价值体现在哪些方面？

**提示思路**：用实验 1-2 的「决策 vs 执行」二分法：模型内化的是哪一半？另一半里有哪些职责（治理、合规、验证、跨系统）模型很难内化？再想框架的价值是变薄了还是位移了。

**我的回答**：

---

### 思考题 4 ★★

> 消融实验中 “工具结果反馈” 的缺失会让 Agent 反复重试直到耗尽迭代预算。在生产环境中，除了工具结果缺失，还有哪些情况可能让 Agent 陷入这种循环？你会设计怎样的检测和终止机制？

**提示思路**：发散列举「循环但看似在前进」的场景（验证器反复拒绝、环境无状态变化、目标冲突、工具幂等失败）；检测上想「状态是否在推进」的度量，终止上想熔断与回退（对照 §2.5 停止条件）。

**我的回答**：

---

### 思考题 5 ★

> 本章用感知、行动、策略三个维度分析了五个 Agent 产品。请选择一个你日常使用的 AI 产品，用这三个维度进行分析，并思考它的架构设计是否合理。如果由你来设计这个 AI 产品，有哪些改进空间？

**提示思路**：按原书产品表列三列（眼睛/手脚/策略）；「改进空间」优先看它没扩展的观察/动作空间，其次看是否有护栏缺口。

**我的回答**：

---

### 思考题 6 ★★

> 如果你要设计一个专门处理航班订票的客服系统，你会选择工作流模式还是自主 Agent 模式？有没有可能在同一个系统中混合使用两种模式？

**提示思路**：对照书中订机票的两种写法——身份核实/支付这类合规关键步骤要确定性，改签/航班取消这类例外要灵活性；想清楚每条路径的失败代价。

**我的回答**：

---

### 思考题 7 ★★★

> 护栏部分提到了工具风险评级。如果一个工具在大多数情况下是低风险的，但在特定参数组合下变为高风险（如 `delete_file` 删除普通文件 vs 删除系统文件），你会如何设计动态风险评估？

**提示思路**：风险是「参数 × 目标对象 × 调用上下文」的函数而非工具固有属性；想想评级结果如何接入执行层（上下文之外的复核），以及怎样避免误拒绝拖慢正常路径。

**我的回答**：

---

### 思考题 8 ★★

> 本章的 Agent 产品表格中，所有 Agent 的动作空间都是 “开放式” 的。一个受限的动作空间（比如只能从预定义选项中选择）在什么场景下反而优于开放式？

**提示思路**：反向思考开放式的代价（出错面/攻击面/不可审计性）；当任务全集可预先枚举、安全与合规压倒灵活性时受限空间胜出——试举一个具体业务。

**我的回答**：

---

### 思考题 9 ★★

> 人工干预机制要求 Agent 能 “优雅地移交控制”。但在实践中，用户可能不在线、响应很慢、或者给出模糊的指令。此时 Agent 应该怎么办？

**提示思路**：把「人工」当作一个不可靠的异步通道来设计：等待策略（阻塞 vs 排队）、超时后的默认动作（安全兜底）、升级路径（换人/降级/挂起），以及如何用上下文记录待决状态。

**我的回答**：

---

### 思考题 10 ★★★

> 引言指出 “好的设计原则应该穿越模型的迭代周期”，但实现这些原则的具体工程手段可能会随模型能力进步而过时。试举一个这样的 Agent 工程手段，并说明理由。

**提示思路**：先分辨「原则」与「手段」——手段通常是为模型某个短板打的补丁（适配层），论证它被哪类模型能力取代、判断依据是什么；也可以反着找：哪些手段看似临时、实际长期有效？

**我的回答**：

---

*精读日期：2026-09-08 · 题目原文出自 `D:\ObsidianRepository\book\chapter1.md`「思考题」节。内容理解见 [[内容精读/第1章 AI Agent 入门]]。*
