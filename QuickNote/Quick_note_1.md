1.1 补充什么是 RL，简要说明，再回到经典的强化学习和控制论视角下的 agent 解释
1.1.1 重点说明了：==产品能力的演进往往就是观察空间和动作空间的演进==
模型即 Agent: 先进模型通过后训练（特别是强化学习）将工具调用能力内化为原生能力：何时调用工具、调哪个、传什么参数，都由模型自己决定，无需人工编排。但这并不意味着框架层变得不重要了。恰恰相反，模型越强大，围绕模型构建的 Harness 就越关键。
1.1.5 中出现了 sdk 的概念，解释这个概念并对比它与 api 这个概念的区别
2.1 前文说Agent = LLM + 上下文 + 工具，而现在又提出了Agent = Model + Harness 这个方程，可以说 harness 就是在上下文 + 工具的基础上加入**约束 + 验证 + 纠正**等规范，从而使 agent 变成一个可以用作在生产系统的完整治理层吗？
我的理解是 LLM + 上下文 + 工具只是 agent 构成的最小工程公式，而通过 harness 中将重心放在约束、验证与纠正上面，更能保证工具调用的安全性，也就是书中所说的“**行业正在从“能做事”向“可靠地做事”转变，Harness 工程因此成为 Agent 系统的核心竞争力。**”
再总结一下五条贯穿全熟的设计模式