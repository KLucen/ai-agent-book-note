结合原文内容简要介绍Transformer 模型并引入 KV Cache

**位置偏好**（Position Bias）[2](https://bojieli.github.io/ai-agent-book/book/chapter2/#fn:lost-in-the-middle)：模型对上下文开头和结尾的信息分配了更高的注意力，中间部分则更容易被忽视。因此，在设计上下文时，把最关键的信息放在开头或结尾是一项重要的实践原则。
使用一个表格对比 KV Cache 和 缓存命中

提示词工程：从语气和风格上可以决定用户的体验，同时控制 LLM 的回复长短可以避免模型过度思考等；结构化的提示可以让模型理解得更加清晰；流程驱动和规则的制定可以让模型知道自己处在什么阶段，以及当前阶段的目标是什么；而规则的细化则可以解放模型的认知资源，使其专注于真正需要思考的部分（就像原文中所说的：在优秀的 Agent 公司里，提示词一般由**产品经理**来设计，基于线上数据分析、用户反馈和运营经验来迭代优化规则定义。工程师的角色是将规则准确地编码到提示词中，确保格式正确、结构清晰，但不应擅自决定业务逻辑。）； Few-shot 的方法则可以让模型通过几个高质量的输入和输出示例“临时学会”回答时的语气，规则等内容。

**提示注入**（Prompt Injection）：攻击者通过 Agent 处理的外部内容（网页、邮件、文档等），将伪装成系统指令的文本混入上下文，从而劫持 Agent 的行为。 以及如何防御提示注入

详细地单独编写一份如何写一份 skills 的 md 文件放置在 DeepReadNote 文件夹中 中，并最好通过一份 skill 的示例（比如Frontend Design，npx skills add https://github.com/anthropics/skills --skill frontend-design）对照着分析如何构建的。

agent 状态栏本质：**把分散在上下文各处的隐式状态提炼为可直接使用的显式知识**

没有很理解这一部分的公式，状态的新增后缀是什么意思，两种方式的计算公式是怎么来的。设每条状态 $S$ token、两次更新间新增后缀 $R$ token、更新 $N$ 次、缓存输入单价为普通输入的 $\alpha$ 倍，则 $C_{\text{替换}} \approx (N-1)(1-\alpha)R$，$C_{\text{追加}} \approx \alpha S N(N-1)/2$；**当 $\alpha SN/2 < (1-\alpha)R$ 时倾向实现二，否则倾向实现一**（未计上下文占用与陈旧状态歧义，实际需结合服务商计费与实测命中率）。